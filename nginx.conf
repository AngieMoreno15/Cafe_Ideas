# Servidor web para frontend de la aplicación Café Ideas
# Todas las rutas son manejadas por React (SPA)
server {
  listen 80;
  server_name localhost;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri /index.html;
  }
}